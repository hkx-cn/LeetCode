#pragma once
#include <string>
using namespace std;
class Solution {
public:
    bool isNumber(char c);
    bool isNumber(string s);
    bool isDecimal(string s);
    bool isInteger(string s);
    bool isIntegerWithoutSing(string s);
};
